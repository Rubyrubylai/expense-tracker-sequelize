<div class="form">
    <form action="/records/new" method="POST">
    <center>
        <input type="button" class="btn deposit-unchecked" id="deposit" value="收入">
        <input type="button" class="btn deduct-checked" id="deduct" value="支出">
        <input type="hidden" name="balance" id="balance" value="支出">
    </center>     
        <div class="row my-4">
            <div class="col-2"><label for="date">日期*:</label></div>
            <div class="col-10">
                <input type="date" class="form-control" id="date" name="date" value="{{date}}">
            </div>
        </div>
        <div class="row my-4">
            <div class="col-2"><label for="category">類別*:</label></div>
            <div class="col-10">
                <select class="form-control" id="category" name="category">
                    <option {{#ifEquals category '家居物業'}} selected {{/ifEquals}}>家居物業</option>
                    <option {{#ifEquals category '交通出行'}} selected {{/ifEquals}}>交通出行</option>
                    <option {{#ifEquals category '休閒娛樂'}} selected {{/ifEquals}}>休閒娛樂</option>
                    <option {{#ifEquals category '餐飲食品'}} selected {{/ifEquals}}>餐飲食品</option>
                    <option {{#ifEquals category '其他支出'}} selected {{/ifEquals}}>其他支出</option>
                </select>
            </div>
        </div>
        <div class="row my-4">
            <div class="col-2"><label for="amount">金額*:</label></div>
            <div class="col-10">
                <input type="number" class="form-control" id="amount" name="amount" value="{{amount}}">
            </div>
        </div>
        <div class="row my-4">
            <div class="col-2"><label for="billName">名稱*:</label></div>
            <div class="col-10">
                <input type="text" class="form-control" id="billName" name="name" value="{{name}}">
            </div>
        </div>
        {{>messages}}
        <center><button id="submit" type="submit" class="btn btn-warning btn-lg" style="width: 200px">送出</button></center>
    </form>
</div>

<script src="/index.js"></script>